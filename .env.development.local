# Local override for dev proxy target (highest precedence in Vite for development)
# This ensures the frontend proxies to the live Railway API without running the API locally.
VITE_DEV_API_TARGET=https://api-retrobus-essonne.up.railway.app

# Keep client-side direct API URL empty to force using the proxy
VITE_API_URL=

# Link internal changelog management to the same backend powering the external site
VITE_API_CHANGELOG_ORIGIN=https://attractive-kindness-rbe-serveurs.up.railway.app
VITE_API_CHANGELOG_PATH=api/changelog
